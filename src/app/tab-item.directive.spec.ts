import { TabsItemDirective } from './tab-item.directive';

describe('TabsItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsItemDirective();
    expect(directive).toBeTruthy();
  });
});