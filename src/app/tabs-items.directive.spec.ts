import { TabsItemsDirective } from './tabs-items.directive';

describe('TabsItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsItemsDirective();
    expect(directive).toBeTruthy();
  });
});
